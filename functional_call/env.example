# ============================================================================
# DashScope（通义千问）配置
# ============================================================================
DASHSCOPE_API_KEY=your_dashscope_api_key_here
QWEN_MODEL=qwen-plus
QWEN_TIMEOUT_S=30
# OpenAI 兼容接口 base_url（DashScope）
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ============================================================================
# 机器人（Modbus over SSH tunnel）
# ============================================================================
MODBUS_HOST=localhost
MODBUS_PORT=1502

# ============================================================================
# 服务端配置
# ============================================================================
FC_SERVER_HOST=0.0.0.0
FC_SERVER_PORT=8766

# ============================================================================
# 事件流（进度播报）
# ============================================================================
EVENT_RETENTION_MAX=2000

# ============================================================================
# 路由增强（本地小模型：可选）
# ============================================================================
# 启用 BART + AdaptiveClassifier 本地模型路由（首次启用会下载模型权重）
ENABLE_LOCAL_ROUTER_MODELS=false

# ============================================================================
# 语音端回调推送（主动推送任务事件到语音端）
# ============================================================================
# 语音端回调接口地址 (支持逗号分隔的多个地址，例如同时推送到飞书适配器和机器人语音播报)
# 示例：VOICE_PUSH_URL=http://127.0.0.1:60502/voice/callback,http://127.0.0.1:8866/voice/callback
VOICE_PUSH_URL=http://127.0.0.1:60502/voice/callback

# 是否启用推送（默认 true）
VOICE_PUSH_ENABLED=true

# 推送超时时间（秒，默认 5）
VOICE_PUSH_TIMEOUT_S=5


